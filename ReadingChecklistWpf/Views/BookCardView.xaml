<UserControl x:Class="ReadingChecklistWpf.Views.BookCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReadingChecklistWpf.Views"
             xmlns:components="clr-namespace:ReadingChecklistWpf.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <components:BookCardUc BookName="{Binding BookName}"
                           IsRead="{Binding IsRead}"
                           DateRead="{Binding DateRead}"
                           Tags="{Binding Tags}"/>
</UserControl>
